<footer class="footer">
  <div class="container">
    <div class="row">
      <div class="col-md-10 col-lg-5">
        <div class="footerblock">
          <h5> {{ options.openingstijden_titel }} </h5>
          {{ options.openingstijden }}
        </div>
      </div>
      <div class="col-md-10 col-lg-5">
        <div class="footerblock">
          <h5> {{ options.adres_titel }} </h5>
          {{ options.adres }}
        </div>
      </div>
      <div class="col-md-10 col-lg-5">
        <div class="footerblock">
          <h5> {{ options.plan_titel }} </h5>
          {% include "footermenu.twig" with {'footermenu': footermenu.get_items} %}
        </div>
      </div>
      <div class="col-md-10 col-lg-5">
        <div class="footerblock">
          <h5> {{ options.volgons_titel }}</h5>
          <div class="followus">
            <a href="{{options.facebook}}"><img src="{{theme.link}}/assets/images/facebook.svg"></a>
            <a href="{{options.twitter}}"> <img src="{{theme.link}}/assets/images/twitter.svg"> </a>
            <a href="{{options.instagram}}"> <img src="{{theme.link}}/assets/images/instagram.svg"> </a>
            <a href="{{options.mail}}"> <img src="{{theme.link}}/assets/images/mail.svg"> </a>
          </div>
        </div>
      </div>
    </div>
    <div class="footermenu">
      
    </div>
  </div>
</footer>